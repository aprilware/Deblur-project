<Window x:Class="Deblur.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:Deblur.Controls"
        xmlns:vm="clr-namespace:Deblur.ViewModels"
        xmlns:engine="clr-namespace:Deblur.Engine;assembly=Deblur.Engine"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        xmlns:local="clr-namespace:Deblur"
        Title="Deblur" Height="720" Width="1200"
        AllowDrop="True">
    <Window.DataContext><vm:MainViewModel/></Window.DataContext>
    <Window.Resources>
        <ObjectDataProvider x:Key="BlurTypeValues" MethodName="GetValues" ObjectType="{x:Type sys:Enum}">
            <ObjectDataProvider.MethodParameters>
                <x:Type TypeName="engine:BlurType"/>
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>
    </Window.Resources>

    <Window.InputBindings>
        <!-- ApplicationCommands.Open ships with Ctrl+O built in; SaveAs has no default gesture, wire it here. -->
        <KeyBinding Key="S"          Modifiers="Ctrl" Command="ApplicationCommands.SaveAs"/>

        <KeyBinding Key="R"          Modifiers="Ctrl" Command="{x:Static local:AppCommands.Reset}"/>
        <KeyBinding Key="D0"         Modifiers="Ctrl" Command="{x:Static local:AppCommands.FitToWindow}"/>
        <KeyBinding Key="D1"         Modifiers="Ctrl" Command="{x:Static local:AppCommands.PixelPerfect}"/>
        <KeyBinding Key="OemPlus"    Modifiers="Ctrl" Command="{x:Static local:AppCommands.ZoomIn}"/>
        <KeyBinding Key="OemMinus"   Modifiers="Ctrl" Command="{x:Static local:AppCommands.ZoomOut}"/>
        <KeyBinding Key="F5"                          Command="{x:Static local:AppCommands.RenderFull}"/>
        <KeyBinding Key="F1"                          Command="{x:Static local:AppCommands.ShowShortcuts}"/>
        <KeyBinding Key="Escape"                      Command="{x:Static local:AppCommands.CancelInteraction}"/>
        <KeyBinding Key="Z"          Modifiers="Ctrl" Command="{x:Static local:AppCommands.Undo}"/>
        <KeyBinding Key="Y"          Modifiers="Ctrl" Command="{x:Static local:AppCommands.Redo}"/>
    </Window.InputBindings>

    <Window.CommandBindings>
        <CommandBinding Command="ApplicationCommands.Open"                       Executed="OnOpenExecuted"/>
        <CommandBinding Command="ApplicationCommands.SaveAs"                     Executed="OnSaveAsExecuted"/>
        <CommandBinding Command="{x:Static local:AppCommands.Reset}"             Executed="OnResetExecuted"/>
        <CommandBinding Command="{x:Static local:AppCommands.FitToWindow}"       Executed="OnFitExecuted"/>
        <CommandBinding Command="{x:Static local:AppCommands.PixelPerfect}"      Executed="OnPixelPerfectExecuted"/>
        <CommandBinding Command="{x:Static local:AppCommands.ZoomIn}"            Executed="OnZoomInExecuted"/>
        <CommandBinding Command="{x:Static local:AppCommands.ZoomOut}"           Executed="OnZoomOutExecuted"/>
        <CommandBinding Command="{x:Static local:AppCommands.RenderFull}"        Executed="OnRenderFullExecuted"/>
        <CommandBinding Command="{x:Static local:AppCommands.ShowShortcuts}"     Executed="OnShowShortcutsExecuted"/>
        <CommandBinding Command="{x:Static local:AppCommands.CancelInteraction}" Executed="OnCancelInteractionExecuted"/>
        <CommandBinding Command="{x:Static local:AppCommands.Undo}"              Executed="OnUndoExecuted"              CanExecute="OnCanUndoExecute"/>
        <CommandBinding Command="{x:Static local:AppCommands.Redo}"              Executed="OnRedoExecuted"              CanExecute="OnCanRedoExecute"/>
    </Window.CommandBindings>

    <Grid>
        <DockPanel>
            <Menu DockPanel.Dock="Top">
                <MenuItem Header="_File">
                    <MenuItem Header="_Open..." Command="ApplicationCommands.Open"/>
                    <MenuItem Header="_Save As..." Command="ApplicationCommands.SaveAs" InputGestureText="Ctrl+S"/>
                    <Separator/>
                    <MenuItem Header="E_xit" Click="OnExitClick"/>
                </MenuItem>
                <MenuItem Header="_Help">
                    <MenuItem Header="_Keyboard Shortcuts..." Command="{x:Static local:AppCommands.ShowShortcuts}"/>
                </MenuItem>
            </Menu>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="320"/>
                </Grid.ColumnDefinitions>

                <controls:PreviewCanvas x:Name="Preview" Grid.Column="0"
                                        Source="{Binding PreviewBitmap}"
                                        IsArrowEnabled="{Binding IsMotionSelected}"
                                        Dragging="OnPreviewDragging"
                                        DragCommitted="OnPreviewDragCommitted"
                                        RoiModeEnabled="{Binding RoiEnabled}"
                                        SelectedRoiRect="{Binding SelectedRoiOverlayRect, Mode=OneWay}"
                                        RoiDrawn="PreviewCanvas_RoiDrawn"/>

                <StackPanel Grid.Column="1" Margin="12">
                    <TextBlock Text="Blur type" FontWeight="Bold" Margin="0,0,0,4"/>
                    <ComboBox ItemsSource="{Binding Source={StaticResource BlurTypeValues}}"
                              SelectedItem="{Binding SelectedBlurType}"/>

                    <TextBlock Text="Algorithm" FontWeight="Bold" Margin="0,12,0,4"/>
                    <ComboBox ItemsSource="{Binding Source={StaticResource AlgorithmTypeValues}}"
                              SelectedItem="{Binding SelectedAlgorithm}"/>

                    <Grid Margin="0,12,0,0" Visibility="{Binding IsMotionSelected, Converter={StaticResource BoolToVis}}">
                        <StackPanel>
                            <TextBlock Text="Angle (°)" Margin="0,4,0,0"/>
                            <Slider Minimum="0" Maximum="360" Value="{Binding Angle}"/>
                            <TextBlock Text="{Binding Angle, StringFormat={}{0:0.0}}" HorizontalAlignment="Right"/>

                            <TextBlock Text="Length (px, proxy)" Margin="0,8,0,0"/>
                            <Slider Minimum="0" Maximum="100" Value="{Binding Length}"/>
                            <TextBlock Text="{Binding Length, StringFormat={}{0:0.0}}" HorizontalAlignment="Right"/>

                            <Button Content="Estimate motion" Command="{Binding EstimateMotionCommand}"
                                    Margin="0,8,0,4" IsEnabled="{Binding HasImage}"/>
                            <Border BorderThickness="1" BorderBrush="LightGray" Padding="4"
                                    Visibility="{Binding MotionSuggestion, Converter={StaticResource NullToVis}}">
                                <StackPanel>
                                    <TextBlock TextWrapping="Wrap">
                                        <Run Text="Suggested: "/>
                                        <Run Text="{Binding MotionSuggestion.Length, StringFormat={}{0:F1} px}"/>
                                        <Run Text=" @ "/>
                                        <Run Text="{Binding MotionSuggestion.Angle, StringFormat={}{0:F0}°}"/>
                                        <Run Text=" (conf. "/>
                                        <Run Text="{Binding MotionSuggestion.Confidence, StringFormat={}{0:P0}}"/>
                                        <Run Text=")"/>
                                    </TextBlock>
                                    <TextBlock Foreground="#B00" TextWrapping="Wrap" Margin="0,4,0,0"
                                               Visibility="{Binding MotionSuggestion.Confidence, Converter={StaticResource LowConfToVis}}"
                                               Text="Low confidence — the estimator can't distinguish motion from image structure. Manual sliders recommended."/>
                                    <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                                        <Button Content="Accept" Command="{Binding AcceptMotionSuggestionCommand}" Margin="0,0,4,0"/>
                                        <Button Content="Dismiss" Command="{Binding DismissMotionSuggestionCommand}"/>
                                    </StackPanel>
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </Grid>

                    <Grid Margin="0,12,0,0" Visibility="{Binding IsOutOfFocusSelected, Converter={StaticResource BoolToVis}}">
                        <StackPanel>
                            <TextBlock Text="Radius (px, proxy)" Margin="0,4,0,0"/>
                            <Slider Minimum="0" Maximum="50" Value="{Binding Radius}"/>
                            <TextBlock Text="{Binding Radius, StringFormat={}{0:0.0}}" HorizontalAlignment="Right"/>

                            <Button Content="Estimate radius" Command="{Binding EstimateDefocusCommand}"
                                    Margin="0,8,0,4" IsEnabled="{Binding HasImage}"/>
                            <Border BorderThickness="1" BorderBrush="LightGray" Padding="4"
                                    Visibility="{Binding DefocusSuggestion, Converter={StaticResource NullToVis}}">
                                <StackPanel>
                                    <TextBlock TextWrapping="Wrap">
                                        <Run Text="Suggested: "/>
                                        <Run Text="{Binding DefocusSuggestion.Radius, StringFormat={}{0:F1} px}"/>
                                        <Run Text=" (conf. "/>
                                        <Run Text="{Binding DefocusSuggestion.Confidence, StringFormat={}{0:P0}}"/>
                                        <Run Text=")"/>
                                    </TextBlock>
                                    <TextBlock Foreground="#B00" TextWrapping="Wrap" Margin="0,4,0,0"
                                               Visibility="{Binding DefocusSuggestion.Confidence, Converter={StaticResource LowConfToVis}}"
                                               Text="Low confidence — no clear disc-PSF zero found. Manual sliders recommended."/>
                                    <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                                        <Button Content="Accept" Command="{Binding AcceptDefocusSuggestionCommand}" Margin="0,0,4,0"/>
                                        <Button Content="Dismiss" Command="{Binding DismissDefocusSuggestionCommand}"/>
                                    </StackPanel>
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </Grid>

                    <Grid Margin="0,12,0,0" Visibility="{Binding IsGaussianSelected, Converter={StaticResource BoolToVis}}">
                        <StackPanel>
                            <TextBlock Text="Sigma (px, proxy)" Margin="0,4,0,0"/>
                            <Slider Minimum="0" Maximum="10" Value="{Binding Sigma}"/>
                            <TextBlock Text="{Binding Sigma, StringFormat={}{0:0.0}}" HorizontalAlignment="Right"/>
                        </StackPanel>
                    </Grid>

                    <StackPanel Margin="0,12,0,0" Visibility="{Binding HasImage, Converter={StaticResource BoolToVis}}">
                        <TextBlock Text="{Binding SelectedAlgorithm, Converter={StaticResource AlgLabel}}" Margin="0,4,0,0"/>
                        <Slider Minimum="0.0001" Maximum="0.1" SmallChange="0.0005" Value="{Binding Smoothness}"/>
                        <TextBlock Text="{Binding Smoothness, StringFormat={}{0:0.0000}}" HorizontalAlignment="Right"/>

                        <CheckBox Content="Deblur selected region"
                                  IsChecked="{Binding RoiEnabled}"
                                  Margin="0,12,0,4"/>
                        <TextBlock Text="Feather radius"
                                   Margin="0,4,0,0"
                                   IsEnabled="{Binding RoiEnabled}"/>
                        <Slider Minimum="0" Maximum="64"
                                Value="{Binding RoiFeatherRadius}"
                                TickFrequency="4"
                                IsSnapToTickEnabled="True"
                                IsEnabled="{Binding RoiEnabled}"/>
                        <TextBlock Text="{Binding RoiFeatherRadius, StringFormat={}{0} px}"
                                   Margin="0,0,0,4"
                                   IsEnabled="{Binding RoiEnabled}"/>

                        <Button Content="Estimate noise" Command="{Binding EstimateNoiseCommand}"
                                Margin="0,12,0,4" IsEnabled="{Binding HasImage}"/>
                        <Border BorderThickness="1" BorderBrush="LightGray" Padding="4"
                                Visibility="{Binding NoiseSuggestion, Converter={StaticResource NullToVis}}">
                            <StackPanel>
                                <TextBlock TextWrapping="Wrap">
                                    <Run Text="Suggested K: "/>
                                    <Run Text="{Binding NoiseSuggestion.SuggestedK, StringFormat={}{0:E2}}"/>
                                    <Run Text=" (σ "/>
                                    <Run Text="{Binding NoiseSuggestion.SigmaNoise, StringFormat={}{0:F4}}"/>
                                    <Run Text=", conf. "/>
                                    <Run Text="{Binding NoiseSuggestion.Confidence, StringFormat={}{0:P0}}"/>
                                    <Run Text=")"/>
                                </TextBlock>
                                <TextBlock Foreground="#B00" TextWrapping="Wrap" Margin="0,4,0,0"
                                           Visibility="{Binding NoiseSuggestion.Confidence, Converter={StaticResource LowConfToVis}}"
                                           Text="Low confidence — noise is dominant vs signal. Manual K recommended."/>
                                <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                                    <Button Content="Accept" Command="{Binding AcceptNoiseSuggestionCommand}" Margin="0,0,4,0"/>
                                    <Button Content="Dismiss" Command="{Binding DismissNoiseSuggestionCommand}"/>
                                </StackPanel>
                            </StackPanel>
                        </Border>

                        <Button Content="Reset" Margin="0,12,0,0" Command="{x:Static local:AppCommands.Reset}"/>
                        <Button Content="Render full resolution" Margin="0,8,0,0" Command="{x:Static local:AppCommands.RenderFull}"/>
                    </StackPanel>

                    <ProgressBar Margin="0,12,0,0" Height="4" IsIndeterminate="True"
                                 Visibility="{Binding IsPreviewComputing, Converter={StaticResource BoolToVis}}"/>

                    <TextBlock Margin="0,20,0,0" Text="{Binding StatusMessage}" TextWrapping="Wrap" Foreground="#888"/>
                </StackPanel>
            </Grid>
        </DockPanel>
        <controls:BusyOverlay x:Name="Busy"/>
    </Grid>
</Window>
